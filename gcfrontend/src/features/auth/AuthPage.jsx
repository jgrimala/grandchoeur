import React from "react";
import LoginForm from "./components/LoginForm"; // Assuming LoginForm is in the components directory under auth

const AuthPage = () => {
	return (
		<div className="auth-page">
			<LoginForm />
		</div>
	);
};

export default AuthPage;
